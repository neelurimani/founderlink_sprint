server:
  port: ${NOTIFICATION_SERVICE_PORT:8086}

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/notification_service?createDatabaseIfNotExist=true}
    username: ${DB_USERNAME:${MYSQL_USERNAME:root}}
    password: ${DB_PASSWORD:${MYSQL_PASSWORD:12345678}}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${JPA_SHOW_SQL:true}
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
      continue-on-error: true
  cloud:
    openfeign:
      circuitbreaker:
        enabled: true
      client:
        config:
          default:
            connectTimeout: 5000
            readTimeout: 15000
            loggerLevel: full
          startup-service:
            url: ${founderlink.startup-service-url:${STARTUP_SERVICE_URL:}}
          user-service:
            url: ${founderlink.user-service-url:${USER_SERVICE_URL:}}

logging:
  level:
    com.founderlink.notificationservice.client: DEBUG

# For SMTP instead of log delivery, set founderlink.email.delivery=smtp and spring.mail.* (see MailConfig).
founderlink:
  email:
    enabled: true
    delivery: log
    from: ${EMAIL_FROM:no-reply@founderlink.local}
    default-to: ${EMAIL_DEFAULT_TO:}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui

management:
  endpoints:
    web:
      exposure:
        include: health,info
